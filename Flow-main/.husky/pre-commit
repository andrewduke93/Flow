#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# fast safety checks before committing
npm run -s check:conflicts || {
  echo "\nCommit blocked: merge-conflict markers found. Resolve them before committing." >&2
  exit 1
}
