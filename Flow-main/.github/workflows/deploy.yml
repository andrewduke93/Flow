name: Disabled duplicate deploy workflow

# This file is intentionally disabled to avoid duplicate GitHub Pages deployments.
# The active deploy workflow is /.github/workflows/deploy.yml at the repository root.
on: []
